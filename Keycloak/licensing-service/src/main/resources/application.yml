# this file is empty for purpose to config service easier. After configs are done, move it to config server.
spring:
  cloud:
    stream:
      bindings:
        input:
          destination: orgChangeTopic
          content-type: application/json
          group: licensing
      kafka:
        binder:
          brokers:
            - localhost:9092
